apiVersion: networking.gke.io/v1
kind: MultiClusterService
metadata:
  namespace: httpbin
  name: zone-mcs
spec:
  template:
    spec:
      selector:
        app: httpbin
      ports:
      - name: http
        port: 8000
        targetPort: 8000
